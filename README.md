# hiking-routes
A simple DSA-based web project that finds the shortest hiking route between locations using graph algorithms.

Overview:
This project creates a hiking trail map where:

-Locations are nodes
-Paths are edges
-The program finds the shortest route between two points using BFS (Breadth First Search)
